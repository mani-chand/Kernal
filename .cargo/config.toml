[build]
rustflags = ['--cfg=getrandom_backend="windows_legacy"']

# Host-specific settings for the GNU build tools on Windows
[target.x86_64-pc-windows-gnu]
rustflags = [
    "-C", "link-arg=-fuse-ld=lld",
    '--cfg=getrandom_backend="windows_legacy"'
]