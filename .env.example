# Template for the local environment configuration.
# Copy this file to '.env' and adjust the path if QEMU is installed in a non-standard directory.

# Path to the QEMU system emulator binary
QEMU_PATH=C:\Program Files\qemu\qemu-system-x86_64.exe
